import { BannerApp, Menu } from '../../config/model/gnConfig';

export const DEFAULT_MENU_APP_CONFIGURATION: Menu = {
  enabled: true,
};

export const DEFAULT_BANNER_APP_CONFIGURATION: BannerApp = {
  enabled: true,
  background: '',
  title: 'home.title',
  subTitle: 'home.subtitle',
  textColor: '#ffffff',
};
